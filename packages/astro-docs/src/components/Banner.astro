---
// Override hook: an optional page banner.
// v1 renders nothing by default. Users supply their own banner by overriding
// this component via `components: { Banner: "./src/MyBanner.astro" }` in config.
---
