{
  "name": "astro-docs-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=examples/playground",
    "dev": "npm run dev --workspace=examples/playground",
    "test": "npm run test --workspace=packages/astro-docs"
  }
}
