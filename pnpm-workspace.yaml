packages:
  - "packages/*"
  - "examples/*"

# Install the latest releases immediately, without the new-package age gate.
minimumReleaseAge: 0

allowBuilds:
  esbuild: true
  sharp: true
  "@biomejs/biome": true
